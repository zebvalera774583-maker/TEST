# Как работает сжатие фото "на лету"

## Что такое "сжатие на лету"?

**"Сжатие на лету"** (on-the-fly compression) означает, что изображение обрабатывается **сразу после выбора файла**, **до загрузки на сервер**. Всё происходит в браузере пользователя, без отправки больших файлов на сервер.

---

## Процесс работы (пошагово)

### Шаг 1: Пользователь выбирает фото
```
Пользователь → Выбирает файл (например, 5 МБ, 4000x3000px) → Браузер
```

### Шаг 2: Функция `compressImage` вызывается
```typescript
const compressedFile = await compressImage(file);
```
Это происходит в функции `handleUpload` (строка 273 в `app/page.tsx`).

### Шаг 3: Библиотека `browser-image-compression` обрабатывает файл

**Что делает библиотека:**

1. **Читает файл в память браузера**
   - Создает объект `Image` из файла
   - Загружает изображение в память

2. **Изменяет размер (ресайз)**
   - Если изображение больше 1600px по ширине или высоте
   - Уменьшает до 1600px, сохраняя пропорции
   - Пример: 4000x3000px → 1600x1200px

3. **Сжимает качество**
   - Применяет качество 85% (для JPEG/WebP)
   - Удаляет избыточные данные
   - Пример: 5 МБ → ~800 КБ

4. **Удаляет EXIF данные**
   - Удаляет метаданные (GPS, дата съемки, модель камеры и т.д.)
   - Это экономит ~50-200 КБ

5. **Конвертирует в WebP (если возможно)**
   - Пытается создать WebP формат (более эффективный)
   - Если не получается → использует JPEG

6. **Создает новый файл**
   - Возвращает новый `File` объект с сжатым изображением
   - Размер файла значительно меньше оригинала

### Шаг 4: Сжатый файл загружается на сервер
```typescript
await supabase.storage.from('Test').upload(fileName, compressedFile, ...)
```
Теперь на сервер отправляется уже сжатый файл (например, 800 КБ вместо 5 МБ).

---

## Параметры сжатия (что они делают)

### `maxSizeMB: 1.5`
- **Что делает:** Ограничивает максимальный размер файла до 1.5 МБ
- **Как работает:** Если после ресайза и сжатия файл всё ещё больше 1.5 МБ, библиотека дополнительно снижает качество
- **Зачем:** Гарантирует, что файл не будет слишком большим

### `maxWidthOrHeight: 1600`
- **Что делает:** Уменьшает изображение, если оно больше 1600px
- **Как работает:** Сохраняет пропорции. Если фото 4000x3000px → становится 1600x1200px
- **Зачем:** Большие изображения занимают много места и медленно загружаются. 1600px достаточно для качественного отображения на экранах

### `initialQuality: 0.85`
- **Что делает:** Устанавливает качество сжатия 85%
- **Как работает:** 100% = без потерь, 0% = максимальное сжатие. 85% = баланс между качеством и размером
- **Зачем:** При 85% визуально потери качества почти незаметны, но размер файла уменьшается в 3-5 раз

### `exifOrientation: false`
- **Что делает:** Удаляет EXIF данные из изображения
- **Как работает:** EXIF содержит метаданные (GPS координаты, дата, модель камеры и т.д.)
- **Зачем:** 
  - Экономит место (50-200 КБ)
  - Защищает приватность (удаляет GPS координаты)
  - Ускоряет обработку

### `useWebWorker: true`
- **Что делает:** Использует Web Worker для обработки
- **Как работает:** Обработка происходит в отдельном потоке, не блокируя интерфейс
- **Зачем:** Пользователь может продолжать работать, пока фото сжимается

### `fileType: 'image/webp'`
- **Что делает:** Пытается создать WebP формат
- **Как работает:** WebP — современный формат, который даёт на 25-35% меньше размер при том же качестве
- **Зачем:** Ещё больше уменьшает размер файла

---

## Пример работы

### До сжатия:
```
Файл: photo.jpg
Размер: 5.2 МБ
Разрешение: 4000x3000px
Формат: JPEG
EXIF: есть (150 КБ метаданных)
```

### После сжатия:
```
Файл: photo.webp (или photo.jpg)
Размер: ~800 КБ (уменьшение в 6.5 раз!)
Разрешение: 1600x1200px
Формат: WebP (или JPEG)
EXIF: удален
```

**Результат:** Файл стал в **6.5 раз меньше**, но визуально качество почти не изменилось!

---

## Где происходит сжатие?

### ✅ В браузере пользователя (client-side)
- Файл обрабатывается **до отправки на сервер**
- Используется JavaScript библиотека `browser-image-compression`
- Работает через HTML5 Canvas API

### ❌ НЕ на сервере
- Сервер получает уже сжатый файл
- Не нужно обрабатывать большие файлы на сервере
- Экономия трафика и времени

---

## Преимущества "сжатия на лету"

1. **Быстрая загрузка**
   - Меньший размер = быстрее отправка на сервер
   - Быстрее загрузка для посетителей сайта

2. **Экономия места**
   - На сервере хранятся сжатые файлы
   - Экономия места в Supabase Storage

3. **Экономия трафика**
   - Меньше данных передается по сети
   - Важно для мобильных пользователей

4. **Автоматизация**
   - Администратору не нужно сжимать фото вручную
   - Всё происходит автоматически

5. **Единообразие**
   - Все фото имеют одинаковые параметры
   - Одинаковое качество и размер

---

## Технические детали

### Библиотека: `browser-image-compression`

**Как она работает внутри:**

1. **Создает Canvas элемент** (невидимый)
   ```javascript
   const canvas = document.createElement('canvas');
   const ctx = canvas.getContext('2d');
   ```

2. **Загружает изображение**
   ```javascript
   const img = new Image();
   img.src = URL.createObjectURL(file);
   ```

3. **Рисует на Canvas с новым размером**
   ```javascript
   canvas.width = 1600;  // Новый размер
   canvas.height = 1200;
   ctx.drawImage(img, 0, 0, 1600, 1200);
   ```

4. **Конвертирует Canvas в файл**
   ```javascript
   canvas.toBlob(blob => {
     // blob - это сжатое изображение
   }, 'image/webp', 0.85);
   ```

5. **Создает новый File объект**
   ```javascript
   const compressedFile = new File([blob], file.name, {
     type: 'image/webp'
   });
   ```

### Fallback механизм

Если что-то не работает, функция использует запасные варианты:

1. **Попытка 1:** WebP с полными настройками
2. **Попытка 2:** JPEG с полными настройками
3. **Попытка 3:** Базовые настройки (без WebP, без EXIF)
4. **Попытка 4:** Возвращает оригинальный файл (если всё сломалось)

---

## Визуальная схема процесса

```
┌─────────────────────────────────────────────────────────┐
│ 1. Пользователь выбирает фото (5 МБ, 4000x3000px)     │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│ 2. compressImage() вызывается                          │
│    └─> browser-image-compression начинает работу       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Обработка в браузере (Web Worker):                  │
│    • Ресайз: 4000x3000px → 1600x1200px                 │
│    • Сжатие качества: 100% → 85%                       │
│    • Удаление EXIF: -150 КБ                            │
│    • Конвертация в WebP                                │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Результат: новый файл (800 КБ, 1600x1200px, WebP)   │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│ 5. Загрузка на Supabase Storage (800 КБ вместо 5 МБ)   │
└─────────────────────────────────────────────────────────┘
```

---

## Важные моменты

### ⚠️ Сжатие происходит ДО загрузки
- Файл обрабатывается в браузере
- На сервер отправляется уже сжатый файл
- Оригинальный файл НЕ сохраняется

### ⚠️ Асинхронная операция
- `compressImage` возвращает `Promise<File>`
- Используется `await` для ожидания результата
- Пока фото сжимается, показывается прогресс загрузки

### ⚠️ Web Worker не блокирует интерфейс
- Обработка происходит в фоновом потоке
- Пользователь может продолжать работать
- Интерфейс не "зависает"

---

## Итог

**Сжатие "на лету"** — это автоматическая оптимизация изображений прямо в браузере перед загрузкой на сервер. Это обеспечивает:

- ✅ Быструю загрузку страниц
- ✅ Экономию места на сервере
- ✅ Единообразное качество фото
- ✅ Автоматизацию процесса
- ✅ Защиту приватности (удаление EXIF)

Всё происходит автоматически, без участия администратора! 🚀

