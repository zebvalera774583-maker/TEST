# Полное описание сайта "Ашот мебель"

## Общая информация

**Домен:** `https://ashot-zebelyan.ru`  
**Технологии:** Next.js 16, React 19, TypeScript, Supabase (Database + Storage), Timeweb App Platform  
**Тип:** Фотогалерея мебели с админ-панелью

---

## 📱 Публичная часть сайта

### Главная страница (`/`)

#### 1. Заголовок профиля
- **Имя профиля:** `ashot.zebelyan` (отображается в левом верхнем углу)

#### 2. Профильный раздел

**Аватарка:**
- Круглое фото профиля (60x60 пикселей)
- Для обычных посетителей: только просмотр
- Для администраторов: можно загрузить/изменить (клик по аватарке открывает диалог выбора файла)

**Статистика (3 колонки):**
- **40** - уникальных кейсов
- **2578** - проектов
- **4** - города

#### 3. Информация об услугах
Отображается в виде списка:
- Проектная реализация
- Дизайн интерьера
- Мебель на заказ
- Комплектация

#### 4. География работы
- **Города:** Москва - Питер - Сочи - Краснодар

#### 5. Кнопки действий (всегда видны)
Три кнопки в одну строку, цвет RAL 5000 (#485B78), ширина соответствует ширине фото-сетки:
- **Редактировать** - кнопка (функционал пока не реализован, показывает alert)
- **Поделиться** - кнопка (функционал пока не реализован, показывает alert)
- **Связаться** - кнопка (функционал пока не реализован, показывает alert)

#### 6. Галерея фотографий

**Сетка фотографий (3 колонки, как в Instagram):**
- Фото отображаются в сетке 3xN (3 колонки)
- Отступ между фото: 2px
- Новые фото/группы фото отображаются сверху
- Каждое фото имеет квадратный формат (aspect ratio 1:1)

**Группы фото (карусели):**
- Фото, загруженные одновременно, группируются в карусель
- В сетке показывается первое фото из группы
- Индикатор количества: `X/Y` (например, `1/5`) в правом верхнем углу квадрата
- Навигация по карусели в миниатюре:
  - Стрелки влево/вправо (‹ ›) появляются при наведении
  - Клик по стрелке переключает фото внутри квадрата
- Клик по квадрату открывает полноэкранный просмотр всей группы

**Одиночные фото:**
- Если фото загружено одно, оно отображается как обычный квадрат
- Клик открывает полноэкранный просмотр

#### 7. Полноэкранный просмотр фото

**Открытие:**
- Клик по любому фото в сетке открывает полноэкранный режим
- Темный фон (rgba(0, 0, 0, 0.95))

**Навигация:**
- **На десктопе:**
  - Видны части соседних фото (80% ширина текущего фото, по 2% отступы с каждой стороны)
  - Стрелки влево/вправо для переключения
  - Клавиатура: `←` `→` для переключения, `Esc` для закрытия
- **На мобильных устройствах:**
  - Одно фото на весь экран (100% ширины)
  - Боковые части соседних фото скрыты
  - Свайп влево/вправо для переключения
  - Кнопка закрытия (×) в правом верхнем углу

**Подпись к фото:**
- Отображается под фото в полноэкранном режиме
- Если у фото есть подпись (caption) - показывается она
- Если подписи нет - показывается "Фото X из Y"
- Белый текст на темном фоне

**Закрытие:**
- Клик вне области фото
- Кнопка × в правом верхнем углу
- Клавиша `Esc`

#### 8. Счетчик просмотров
- При каждом открытии страницы автоматически увеличивается счетчик просмотров в базе данных
- Счетчик не отображается на странице (используется для статистики)

---

## 🔐 Админ-панель

### Страница входа (`/admin`)

**Описание:**
- Отдельная страница для входа в админ-панель
- Центрированная форма на белом фоне
- Заголовок: "Ашот мебель"
- Подзаголовок: "Вход в админ-панель"

**Функционал:**
- Поле для ввода пароля (тип `password`)
- Кнопка "Войти"
- Обработка ошибок (красная обводка поля при ошибке, сообщение об ошибке)
- Автоматическое перенаправление на главную после успешного входа
- Если пользователь уже авторизован - автоматическое перенаправление на главную
- Ссылка "← Вернуться на главную"

**Авторизация:**
- Пароль хранится в переменной окружения `ADMIN_PASSWORD`
- После успешного входа в `localStorage` сохраняется флаг `admin_authenticated = 'true'`
- Авторизация сохраняется между сессиями (до очистки localStorage)

---

### Функции администратора на главной странице

После авторизации администратор видит дополнительные элементы управления:

#### 1. Кнопка "Выйти"
- Отображается в правом верхнем углу (маленькая красная кнопка)
- При клике выходит из админ-панели (очищает `localStorage`)

#### 2. Загрузка аватарки
- Клик по аватарке открывает диалог выбора файла
- Поддерживаются форматы: все изображения (`image/*`)
- После выбора файл автоматически загружается
- Изображение сжимается перед загрузкой
- После успешной загрузки показывается уведомление

#### 3. Загрузка фотографий

**Процесс загрузки:**

1. **Нажатие кнопки "Загрузить фото":**
   - Синяя кнопка с иконкой 📷
   - Появляется форма с полем для подписи

2. **Ввод подписи (необязательно):**
   - Поле: "Подпись к фото (необязательно)"
   - Подсказка: "Подпись будет добавлена ко всем выбранным фото"
   - Можно ввести текст или оставить пустым

3. **Выбор фото:**
   - Кнопка "Выбрать фото" открывает диалог выбора файлов
   - Можно выбрать несколько фото одновременно (`multiple`)
   - Поддерживаются форматы: все изображения (`image/*`)

4. **Автоматическая обработка:**
   - Каждое фото автоматически сжимается:
     - Максимальный размер: 2 МБ
     - Максимальные размеры: 1920x1920 пикселей
   - Все выбранные фото загружаются в одну группу (один `group_id`)
   - Подпись (если указана) добавляется ко всем фото в группе
   - Показывается прогресс загрузки (процент и прогресс-бар)

5. **Результат:**
   - После загрузки все фото появляются в галерее как одна группа
   - Фото автоматически сортируются по порядку загрузки
   - Форма с подписью скрывается, возвращается кнопка "Загрузить фото"

**Отмена загрузки:**
- Кнопка "Отмена" возвращает к исходному виду (скрывает форму с подписью)

#### 4. Удаление фотографий

**В миниатюре (в сетке):**
- Кнопка × в левом верхнем углу квадрата с группой фото
- Видна только администратору
- При клике появляется подтверждение: "Удалить эту группу фото?"
- При подтверждении удаляются все фото из группы

**Примечание:** Функция переупорядочивания фото (handleMove) существует в коде, но не отображается в интерфейсе.

---

## 📊 База данных (Supabase)

### Таблица `site_photos`
Хранит информацию о фотографиях:
- `id` (UUID) - уникальный идентификатор
- `public_url` (TEXT) - публичная ссылка на фото в Supabase Storage
- `sort_order` (INTEGER) - порядок сортировки
- `group_id` (TEXT) - идентификатор группы (фото, загруженные вместе, имеют одинаковый group_id)
- `caption` (TEXT) - подпись к фото (необязательно)
- `created_at` (TIMESTAMPTZ) - дата и время создания

### Таблица `site_stats`
Хранит статистику сайта:
- `id` (INTEGER) - всегда 1
- `views_total` (BIGINT) - общее количество просмотров страницы
- `avatar_url` (TEXT) - ссылка на аватарку профиля
- `updated_at` (TIMESTAMPTZ) - дата последнего обновления

### Политики безопасности (RLS)
- **Публичное чтение:** Все пользователи могут просматривать фото и статистику
- **Запись/удаление:** Только через API с использованием `service_role` ключа (требуется пароль администратора)

---

## 🎨 Дизайн и стили

### Цветовая схема
- **Основной цвет кнопок:** RAL 5000 (#485B78) - темно-серый с синеватым оттенком
- **Hover кнопок:** #5a6f8f (более светлый оттенок)
- **Кнопка загрузки:** #0070f3 (синий)
- **Кнопка выхода:** #dc3545 (красный)
- **Фон:** Белый (#ffffff)
- **Текст:** Темно-серый (#333)

### Типографика
- **Шрифт:** Arial, Helvetica, sans-serif
- **Заголовок профиля:** 18px, bold
- **Статистика:** 18px (числа), 12px (подписи)
- **Кнопки:** 14-16px, font-weight 500

### Адаптивность
- Максимальная ширина контента: 800px (центрировано)
- Адаптивная сетка фото: всегда 3 колонки на десктопе
- Полноэкранный просмотр: разное поведение на мобильных (100% ширины) и десктопе (80% с боковыми частями)

---

## 🔧 Технические особенности

### Авторизация
- Хранится в `localStorage` (ключ: `admin_authenticated`)
- Не требует серверных сессий
- Сохраняется между перезагрузками страницы
- Очищается при нажатии "Выйти" или очистке localStorage

### Загрузка файлов
- Используется библиотека `browser-image-compression` для сжатия изображений
- Загрузка происходит через API route `/api/admin/photos`
- Файлы хранятся в Supabase Storage
- Все загруженные одновременно фото получают одинаковый `group_id`

### Группировка фото
- Фото с одинаковым `group_id` отображаются как карусель
- Фото без `group_id` отображаются как одиночные (для старых загрузок)
- Группы сортируются по дате создания (новые сверху)
- Фото внутри группы сортируются по `sort_order`

### Счетчик просмотров
- Автоматически увеличивается при каждой загрузке страницы
- Использует API route `/api/stats/increment`
- Обновляет таблицу `site_stats` в базе данных

---

## 📝 Примечания

1. **Кнопки "Редактировать", "Поделиться", "Связаться":** В данный момент функционал не реализован (показывают alert). Эти кнопки всегда видны всем посетителям.

2. **Статистика (40 кейсов, 2578 проектов, 4 города):** Жестко закодирована в интерфейсе, не берется из базы данных.

3. **Услуги и города:** Текст жестко закодирован в интерфейсе.

4. **Переупорядочивание фото:** Функция `handleMove` существует в коде, но не используется в интерфейсе.

5. **Удаление фото:** Удаляется из базы данных и, предположительно, из Supabase Storage (нужно проверить реализацию API route).

---

## 🚀 Деплой

- **Платформа:** Timeweb App Platform
- **Автодеплой:** При каждом push в ветку `main` на GitHub
- **Домен:** `ashot-zebelyan.ru`
- **SSL:** Автоматически настроен
- **Переменные окружения:**
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `ADMIN_PASSWORD`

---

**Дата последнего обновления:** Январь 2025

