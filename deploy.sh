#!/bin/bash

# Скрипт для деплоя на Yandex Cloud

echo "🚀 Начинаем деплой..."

# Переходим в директорию проекта
cd /var/www/ashot-zebelyan || exit

# Получаем последние изменения из git
echo "📥 Получаем изменения из Git..."
git pull origin main

# Устанавливаем зависимости
echo "📦 Устанавливаем зависимости..."
npm ci --production

# Собираем проект
echo "🔨 Собираем проект..."
npm run build

# Перезапускаем приложение через PM2
echo "🔄 Перезапускаем приложение..."
pm2 restart ashot-zebelyan || pm2 start ecosystem.config.js

echo "✅ Деплой завершен!"

