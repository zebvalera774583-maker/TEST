# Автоматический деплой на Timeweb App Platform

## Преимущества авто-деплоя

✅ **Простота** - не нужно настраивать сервер вручную  
✅ **Автоматические обновления** - при пуше в GitHub сайт обновится автоматически  
✅ **Управление через панель** - всё через веб-интерфейс  
✅ **SSL автоматически** - сертификаты настраиваются автоматически  
✅ **Масштабирование** - можно увеличить ресурсы при необходимости  

## Шаг 1: Регистрация в Timeweb Cloud

1. Зайдите на [timeweb.cloud](https://timeweb.cloud)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **App Platform** (или **Приложения**)

## Шаг 2: Подключение GitHub репозитория

1. В панели Timeweb Cloud нажмите **"Создать приложение"** или **"Новое приложение"**
2. Выберите источник: **GitHub**
3. Авторизуйтесь в GitHub (если нужно)
4. Выберите репозиторий: `zebvalera774583-maker/TEST`
5. Выберите ветку: `main`
6. Выберите папку: `test` (важно! Проект находится в папке `test`)

## Шаг 3: Настройка сборки

Timeweb должен автоматически определить Next.js. Проверьте настройки:

**Build Command (команда сборки):**
```bash
npm install && npm run build
```

**Start Command (команда запуска):**
```bash
npm start
```

**Root Directory (корневая директория):**
```
test
```

**Node.js Version:**
Выберите версию 20.x (или 18.x)

## Шаг 4: Настройка переменных окружения

В разделе **Environment Variables** (Переменные окружения) добавьте:

```
NEXT_PUBLIC_SUPABASE_URL=ваш_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
ADMIN_PASSWORD=ваш_пароль_админа
NODE_ENV=production
```

**Где взять значения:**
- `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` - из панели Supabase (Settings → API)
- `SUPABASE_SERVICE_ROLE_KEY` - из панели Supabase (Settings → API → service_role key)
- `ADMIN_PASSWORD` - ваш пароль для входа в админку (придумайте)

## Шаг 5: Настройка домена

1. В настройках приложения найдите раздел **"Домены"** или **"Domains"**
2. Добавьте домен: `ashot-zebelyan.ru`
3. Добавьте домен: `www.ashot-zebelyan.ru`
4. Timeweb покажет вам DNS записи, которые нужно добавить

## Шаг 6: Настройка DNS в nic.ru

1. Войдите в панель управления доменом в nic.ru
2. Перейдите в **Управление DNS-записями**
3. Удалите старые записи для Vercel (A и CNAME)
4. Добавьте DNS записи, которые показал Timeweb (обычно это CNAME записи)

**Пример DNS записей (точные значения покажет Timeweb):**

CNAME запись для основного домена:
- **Тип:** CNAME
- **Имя:** `@` (или `ashot-zebelyan.ru`)
- **Значение:** `ваш_домен.timeweb.cloud` (Timeweb покажет точное значение)
- **TTL:** 3600

CNAME запись для www:
- **Тип:** CNAME
- **Имя:** `www`
- **Значение:** `ваш_домен.timeweb.cloud` (или можно `ashot-zebelyan.ru`)
- **TTL:** 3600

5. Сохраните изменения
6. Подождите 10-30 минут для распространения DNS

## Шаг 7: Запуск деплоя

1. В панели Timeweb нажмите **"Развернуть"** или **"Deploy"**
2. Дождитесь завершения сборки (обычно 3-5 минут)
3. После успешной сборки приложение будет доступно по домену

## Шаг 8: Проверка

1. Подождите 10-30 минут после настройки DNS
2. Откройте сайт: `https://ashot-zebelyan.ru`
3. Проверьте работу админки: `https://ashot-zebelyan.ru/admin`
4. Проверьте загрузку фото

## Автоматические обновления

После настройки авто-деплоя:
- Каждый раз, когда вы делаете `git push` в ветку `main`
- Timeweb автоматически:
  1. Получит изменения из GitHub
  2. Установит зависимости (`npm install`)
  3. Соберет проект (`npm run build`)
  4. Запустит новую версию
  5. Сайт обновится автоматически!

**Это намного удобнее, чем VPS!** 🎉

## Управление приложением

В панели Timeweb вы можете:
- Просматривать логи приложения
- Управлять переменными окружения
- Масштабировать ресурсы (RAM, CPU)
- Просматривать статистику использования
- Настраивать автоматические бэкапы

## Полезные ссылки

- [Документация Timeweb App Platform](https://timeweb.cloud/docs/apps/)
- [Деплой Next.js на Timeweb](https://timeweb.cloud/docs/apps/deploying-frontend-apps/nextjs)

## Если что-то не работает

1. **Проверьте логи** в панели Timeweb → ваше приложение → Логи
2. **Проверьте переменные окружения** - все ли правильно указаны
3. **Проверьте DNS** - правильно ли настроены записи
4. **Проверьте сборку** - нет ли ошибок при сборке

## Стоимость

Timeweb App Platform обычно стоит от 200-500 ₽/мес в зависимости от ресурсов. Для вашего проекта достаточно минимального тарифа.

